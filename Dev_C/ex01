# include <stdio.h>

int main(){
	//251229

	// main은 지역. {}안에 있는 코드를 하나의 "함수"로 포함함 
	// printf란? 출력문을 의미 => print + format(형식, 양식, 서식) -> "형식에 맞추어 출력한다" 라는 의미 
	// %d는 ""이후 ,가 선언되고 매핑되는 정수를 의미, \n은 줄바꿈을 의미 
 
	//printf("hello c~\n");
	//printf("정수 출력: %d %d\n", 10, 20);
	//출력문 printf() 
	
	int n;
	n = 100; 
	int m;
	m = n;
	
	printf("m에 n의 값을 대입하고 m을 출력.\n");
	printf("n = %d\n\n", n);
	
	// r에 n의 값을 대입하고 m을 더하시오. 
	printf("r에 n의 값을 대입하고 m을 더한 후 r을 출력.\n");
	int r = n + m;
	printf("r = %d\n\n", r);
	
	// 소수점이 출력이 되는가?
	printf("r을 소수점으로 출력하려면?\n"); 
	r = 101;
	r = r / 2.0;
	printf("r = %d\n", r);
	
	// 출력 X. 이유는 r은 int로 저장했기 때문에 소수점이 출력되지 않음. 
	r = 101;
	// R이라는 변수를 새로 만들어 integer가 아닌 double로 지정. 
	double R = r / 2.0;
	printf("R = %.2lf\n\n", R);
	
	
	// if문 
	if(1 == 0){
		printf("참\n\n");
	}else{
		printf("거짓\n\n");
	}
	
	// n 안의 값이 짝수면 짝수, 홀수면 홀수라고 출력되는 if문을 작성.
	n = 100;
	
	printf("1. ==을 사용했을 시");
	if(n % 2 == 0){
		printf("%d는 짝수입니다.\n\n", n);
	} else{
		printf("%d는 홀수입니다.\n\n", n); 
	}
	
	printf("2. !=을 사용했을 시");
	if(n % 2 != 0){
		printf("%d는 짝수입니다.\n\n", n);
	} else{
		printf("%d는 홀수입니다.\n\n", n); 
	}
	
	
	//두 수를 비교해서 큰 숫자가 무엇인지 출력.
	int a, b;
	a = 100;
	b = 100;
	if(a > b){
		printf("더 큰수는 a(%d)입니다.\n\n", a);
	} else if(a < b){
		printf("더 큰수는 b(%d)입니다.\n\n", b);
	}else{
		printf("두 수가 같습니다.\n\n");
	}
	
	
	// 변수 n에 123이 저장되어있다. 100의 자리, 10의 자리, 1의 자리의 수를 각각 적으시오. 
	n = 321;
	int a1 = 0;
	int b1 = 0;
	int c1 = 0;

	a1 = n / 100;
	b1 = n % 100;	
	c1 = b1 % 10;
	b1 = b1 / 10;
	
	// 건엽이형 추천 -> 10의 자리를 구할 때 (n % 100) / 10 을 하면 값은 10의자리, 나머지는 1의 자리. 
	// 100의자리는 OK. 
	// 즉
	/*  
		a1 = n/100;
		b1 = (n % 100) / 10;
		c1 =  (n % 100) % 10;
	*/
	
	
	printf("100의 자리의 수는 %d\n", a1);
	printf("10의 자리의 수는 %d\n", b1);
	printf("1의 자리의 수는 %d\n", c1) ;
	
	/* 설명
		Q) n의 값을 최소 0부터 최대 세자리 수 까지 기입하고, 이후 100의자리, 10의자리, 1의자리를 출력하기. 

		An)
		n에 0~999 사이의 자리 수 대입.
		이후 a1에 n을 100으로 나눈 값 대입. (100의자리 몇인지 저장) 
		이후 b1에 n을 100으로 나눈 나머지 값 대입.
		b1원에 다시 b1을 10으로 나눈 값 대입. (10의자리 몇인지 저장) 
		이후 c1에 b1을 10으로 나눈 나머지 값 대입. (1의자리 몇인지 저장) 
	*/
	
	return 0;
} 
